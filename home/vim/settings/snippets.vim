""""""""Utltisnips""""""""
" we still want to use ultisnippets since fzf will use it for list
" just map expand key to un-used keybind to avoid default <tab> mapping
let g:UltiSnipsExpandTrigger = "<leader>yyy"
"let g:UltiSnipsJumpForwardTrigger = "<c-n>"
"let g:UltiSnipsJumpBackwardTrigger = "<c-p>"
